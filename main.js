var unique = require('uniq');
var Typed = require('typed.js');

var options = {
  strings: ["Improvements Coming. ^1000 Check back soon."],
  typeSpeed: 60,
  loop: true
}

var typed = new Typed(".line", options);
